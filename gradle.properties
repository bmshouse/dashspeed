org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
# Uncomment and set this to a direct download URL for yolov8n_int8.tflite.
# The downloadModel task fetches the file on first build (or when -PforceModelUpdate is passed).
# Example (GitHub Release asset):
#   modelUrl=https://github.com/YOUR_USERNAME/dashspeed-models/releases/download/v1.0/yolov8n_int8.tflite
# modelUrl=
org.gradle.configuration-cache=true
org.gradle.parallel=true
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true
